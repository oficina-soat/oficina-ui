openapi: 3.1.0
info:
  title: oficina-execution-service API
  version: 1.0.0
  description: Contrato REST v1 para catalogo de servicos, pecas, estoque, diagnostico, execucao e reparo.
  license:
    name: Internal
    identifier: LicenseRef-Internal
servers:
  - url: /api/v1
security:
  - bearerAuth: []
tags:
  - name: Servicos
    description: Operacoes de catalogo tecnico de servicos.
  - name: Pecas
    description: Operacoes de catalogo tecnico de pecas.
  - name: Estoque
    description: Operacoes de saldo e movimentos de estoque.
  - name: Execucoes
    description: Operacoes de diagnostico, execucao e reparo.
paths:
  /servicos:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    post:
      tags: [Servicos]
      summary: Criar servico
      operationId: criarServico
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServicoCreateRequest"
            examples:
              servico:
                value:
                  nome: Troca de bateria
                  descricao: Substituicao da bateria
                  valorBase: 80.0
      responses:
        "201":
          description: Servico criado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Servico"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      tags: [Servicos]
      summary: Consultar servicos
      operationId: consultarServicos
      parameters:
        - $ref: "#/components/parameters/NomeFilter"
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
      responses:
        "200":
          description: Servicos encontrados.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServicoPage"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /servicos/{servicoId}:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    get:
      tags: [Servicos]
      summary: Consultar servico
      operationId: consultarServico
      parameters:
        - $ref: "#/components/parameters/ServicoId"
      responses:
        "200":
          description: Servico encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Servico"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags: [Servicos]
      summary: Atualizar servico
      operationId: atualizarServico
      parameters:
        - $ref: "#/components/parameters/ServicoId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServicoUpdateRequest"
      responses:
        "200":
          description: Servico atualizado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Servico"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /pecas:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    post:
      tags: [Pecas]
      summary: Criar peca
      operationId: criarPeca
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PecaCreateRequest"
            examples:
              peca:
                value:
                  nome: Bateria 60Ah
                  codigo: BAT-60AH
                  valorUnitario: 320.0
      responses:
        "201":
          description: Peca criada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Peca"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      tags: [Pecas]
      summary: Consultar pecas
      operationId: consultarPecas
      parameters:
        - $ref: "#/components/parameters/NomeFilter"
        - name: codigo
          in: query
          schema:
            type: string
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
      responses:
        "200":
          description: Pecas encontradas.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PecaPage"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /pecas/{pecaId}:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    get:
      tags: [Pecas]
      summary: Consultar peca
      operationId: consultarPeca
      parameters:
        - $ref: "#/components/parameters/PecaId"
      responses:
        "200":
          description: Peca encontrada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Peca"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags: [Pecas]
      summary: Atualizar peca
      operationId: atualizarPeca
      parameters:
        - $ref: "#/components/parameters/PecaId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PecaUpdateRequest"
      responses:
        "200":
          description: Peca atualizada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Peca"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /estoques/pecas/{pecaId}/saldo:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    get:
      tags: [Estoque]
      summary: Consultar saldo
      operationId: consultarSaldoEstoque
      parameters:
        - $ref: "#/components/parameters/PecaId"
      responses:
        "200":
          description: Saldo encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SaldoEstoque"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /estoques/movimentos:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    get:
      tags: [Estoque]
      summary: Consultar movimentacoes
      operationId: consultarMovimentosEstoque
      parameters:
        - name: pecaId
          in: query
          schema:
            type: string
            format: uuid
        - name: ordemServicoId
          in: query
          schema:
            type: string
            format: uuid
        - name: tipo
          in: query
          schema:
            $ref: "#/components/schemas/TipoMovimentoEstoque"
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
      responses:
        "200":
          description: Movimentacoes encontradas.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MovimentoEstoquePage"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /estoques/movimentos/entrada:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    post:
      tags: [Estoque]
      summary: Registrar entrada
      operationId: registrarEntradaEstoque
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MovimentoEstoqueRequest"
      responses:
        "201":
          $ref: "#/components/responses/MovimentoEstoqueCriado"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /estoques/movimentos/reserva:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    post:
      tags: [Estoque]
      summary: Reservar estoque
      operationId: reservarEstoque
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MovimentoEstoqueRequest"
      responses:
        "201":
          $ref: "#/components/responses/MovimentoEstoqueCriado"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /estoques/movimentos/consumo:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    post:
      tags: [Estoque]
      summary: Consumir estoque
      operationId: consumirEstoque
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MovimentoEstoqueRequest"
      responses:
        "201":
          $ref: "#/components/responses/MovimentoEstoqueCriado"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /estoques/movimentos/estorno:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    post:
      tags: [Estoque]
      summary: Estornar estoque
      operationId: estornarEstoque
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MovimentoEstoqueRequest"
      responses:
        "201":
          $ref: "#/components/responses/MovimentoEstoqueCriado"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /execucoes:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    post:
      tags: [Execucoes]
      summary: Criar execucao
      operationId: criarExecucao
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExecucaoCreateRequest"
            examples:
              execucao:
                value:
                  ordemServicoId: d290f1ee-6c54-4b01-90e6-d701748f0851
      responses:
        "201":
          description: Execucao criada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Execucao"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      tags: [Execucoes]
      summary: Consultar execucoes
      operationId: consultarExecucoes
      parameters:
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/StatusExecucao"
      responses:
        "200":
          description: Execucoes encontradas.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Execucao"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /execucoes/fila:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    get:
      tags: [Execucoes]
      summary: Consultar fila de execucao
      operationId: consultarFilaExecucao
      description: Retorna execucoes pendentes de acao operacional, ordenadas por prioridade crescente e data de criacao.
      parameters:
        - name: status
          in: query
          description: Quando ausente, retorna execucoes em CRIADA e DIAGNOSTICO_CONCLUIDO.
          schema:
            $ref: "#/components/schemas/StatusExecucao"
      responses:
        "200":
          description: Fila de execucao encontrada.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/FilaExecucaoItem"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /execucoes/{execucaoId}:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    get:
      tags: [Execucoes]
      summary: Consultar execucao
      operationId: consultarExecucao
      parameters:
        - $ref: "#/components/parameters/ExecucaoId"
      responses:
        "200":
          description: Execucao encontrada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Execucao"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /ordens-servico/{ordemServicoId}/execucao:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    get:
      tags: [Execucoes]
      summary: Consultar execucao da OS
      operationId: consultarExecucaoDaOrdemServico
      parameters:
        - $ref: "#/components/parameters/OrdemServicoId"
      responses:
        "200":
          description: Execucao da OS encontrada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Execucao"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /execucoes/{execucaoId}/diagnostico/inicio:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    post:
      tags: [Execucoes]
      summary: Iniciar diagnostico
      operationId: iniciarDiagnostico
      parameters:
        - $ref: "#/components/parameters/ExecucaoId"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          description: Diagnostico iniciado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Execucao"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /execucoes/{execucaoId}/diagnostico/conclusao:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    post:
      tags: [Execucoes]
      summary: Concluir diagnostico
      operationId: concluirDiagnostico
      parameters:
        - $ref: "#/components/parameters/ExecucaoId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConclusaoDiagnosticoRequest"
      responses:
        "200":
          description: Diagnostico concluido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Execucao"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /execucoes/{execucaoId}/reparo/inicio:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    post:
      tags: [Execucoes]
      summary: Iniciar reparo
      operationId: iniciarReparo
      parameters:
        - $ref: "#/components/parameters/ExecucaoId"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          description: Reparo iniciado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Execucao"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /execucoes/{execucaoId}/reparo/conclusao:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    post:
      tags: [Execucoes]
      summary: Concluir reparo
      operationId: concluirReparo
      parameters:
        - $ref: "#/components/parameters/ExecucaoId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConclusaoReparoRequest"
      responses:
        "200":
          description: Reparo concluido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Execucao"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /execucoes/{execucaoId}/cancelamento:
    parameters:
      - $ref: "#/components/parameters/CorrelationId"
    post:
      tags: [Execucoes]
      summary: Cancelar execucao
      operationId: cancelarExecucao
      parameters:
        - $ref: "#/components/parameters/ExecucaoId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelamentoRequest"
      responses:
        "200":
          description: Execucao cancelada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Execucao"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    NomeFilter:
      name: nome
      in: query
      description: Trecho do nome sem distinção entre maiúsculas e minúsculas.
      schema:
        type: string
    Page:
      name: page
      in: query
      description: Índice da página, iniciado em zero.
      schema:
        type: integer
        minimum: 0
        default: 0
    Size:
      name: size
      in: query
      description: Quantidade por página, limitada a 100.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
    CorrelationId:
      name: X-Correlation-Id
      in: header
      required: false
      description: Identificador de correlacao aceito do cliente e propagado em chamadas HTTP, eventos, logs e traces, conforme contracts/error-model.md.
      schema:
        type: string
        minLength: 1
        maxLength: 128
    ServicoId:
      name: servicoId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    PecaId:
      name: pecaId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ExecucaoId:
      name: execucaoId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    OrdemServicoId:
      name: ordemServicoId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    IdempotencyKey:
      name: X-Idempotency-Key
      in: header
      required: true
      description: Chave obrigatoria neste servico para retries seguros em operacoes POST ou PATCH com efeito colateral, conforme contracts/idempotency.md.
      schema:
        type: string
        minLength: 8
        maxLength: 128
  headers:
    CorrelationId:
      description: Identificador de correlacao retornado em toda resposta de erro.
      schema:
        type: string
    Traceparent:
      description: Contexto de trace W3C retornado quando houver instrumentacao distribuida.
      schema:
        type: string
    RetryAfter:
      description: Tempo recomendado para nova tentativa quando a chave de idempotencia ainda estiver em processamento.
      schema:
        type: integer
        minimum: 1
  responses:
    MovimentoEstoqueCriado:
      description: Movimento de estoque registrado.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/MovimentoEstoque"
    BadRequest:
      description: Requisicao invalida.
      headers:
        X-Correlation-Id:
          $ref: "#/components/headers/CorrelationId"
        traceparent:
          $ref: "#/components/headers/Traceparent"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unauthorized:
      description: Token JWT ausente, invalido ou expirado.
      headers:
        X-Correlation-Id:
          $ref: "#/components/headers/CorrelationId"
        traceparent:
          $ref: "#/components/headers/Traceparent"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: Recurso nao encontrado.
      headers:
        X-Correlation-Id:
          $ref: "#/components/headers/CorrelationId"
        traceparent:
          $ref: "#/components/headers/Traceparent"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Conflict:
      description: Conflito de estado, duplicidade, saldo ou idempotencia.
      headers:
        X-Correlation-Id:
          $ref: "#/components/headers/CorrelationId"
        traceparent:
          $ref: "#/components/headers/Traceparent"
        Retry-After:
          $ref: "#/components/headers/RetryAfter"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
  schemas:
    ServicoCreateRequest:
      type: object
      required: [nome, valorBase]
      properties:
        nome:
          type: string
        descricao:
          type: string
        valorBase:
          type: number
          format: decimal
          minimum: 0
    ServicoUpdateRequest:
      allOf:
        - $ref: "#/components/schemas/ServicoCreateRequest"
    Servico:
      allOf:
        - $ref: "#/components/schemas/ServicoCreateRequest"
        - type: object
          required: [servicoId, ativo, criadoEm, atualizadoEm]
          properties:
            servicoId:
              type: string
              format: uuid
            ativo:
              type: boolean
            criadoEm:
              type: string
              format: date-time
            atualizadoEm:
              type: string
              format: date-time
    ServicoPage:
      allOf:
        - $ref: "#/components/schemas/PageMetadata"
        - type: object
          required: [items]
          properties:
            items:
              type: array
              items:
                $ref: "#/components/schemas/Servico"
    PecaCreateRequest:
      type: object
      required: [nome, codigo, valorUnitario]
      properties:
        nome:
          type: string
        codigo:
          type: string
        valorUnitario:
          type: number
          format: decimal
          minimum: 0
    PecaUpdateRequest:
      allOf:
        - $ref: "#/components/schemas/PecaCreateRequest"
    Peca:
      allOf:
        - $ref: "#/components/schemas/PecaCreateRequest"
        - type: object
          required: [pecaId, ativo, criadoEm, atualizadoEm]
          properties:
            pecaId:
              type: string
              format: uuid
            ativo:
              type: boolean
            criadoEm:
              type: string
              format: date-time
            atualizadoEm:
              type: string
              format: date-time
    PecaPage:
      allOf:
        - $ref: "#/components/schemas/PageMetadata"
        - type: object
          required: [items]
          properties:
            items:
              type: array
              items:
                $ref: "#/components/schemas/Peca"
    SaldoEstoque:
      type: object
      required: [pecaId, quantidadeDisponivel, quantidadeReservada, atualizadoEm, acoesPermitidas]
      properties:
        pecaId:
          type: string
          format: uuid
        quantidadeDisponivel:
          type: integer
          minimum: 0
        quantidadeReservada:
          type: integer
          minimum: 0
        atualizadoEm:
          type: string
          format: date-time
        acoesPermitidas:
          type: array
          items:
            $ref: "#/components/schemas/AcaoPermitidaEstoque"
    MovimentoEstoqueRequest:
      type: object
      required: [pecaId, quantidade]
      properties:
        pecaId:
          type: string
          format: uuid
        ordemServicoId:
          type: string
          format: uuid
        quantidade:
          type: integer
          minimum: 1
        motivo:
          type: string
    MovimentoEstoque:
      allOf:
        - $ref: "#/components/schemas/MovimentoEstoqueRequest"
        - type: object
          required: [movimentoId, tipo, criadoEm]
          properties:
            movimentoId:
              type: string
              format: uuid
            tipo:
              $ref: "#/components/schemas/TipoMovimentoEstoque"
            criadoEm:
              type: string
              format: date-time
    TipoMovimentoEstoque:
      type: string
      enum: [ENTRADA, RESERVA, CONSUMO, ESTORNO]
    AcaoPermitidaEstoque:
      type: string
      enum: [REGISTRAR_ENTRADA]
    MovimentoEstoquePage:
      allOf:
        - $ref: "#/components/schemas/PageMetadata"
        - type: object
          required: [items]
          properties:
            items:
              type: array
              items:
                $ref: "#/components/schemas/MovimentoEstoque"
    PageMetadata:
      type: object
      required: [page, size, totalElements, totalPages]
      properties:
        page:
          type: integer
          minimum: 0
        size:
          type: integer
          minimum: 1
        totalElements:
          type: integer
          format: int64
          minimum: 0
        totalPages:
          type: integer
          minimum: 0
    ExecucaoCreateRequest:
      type: object
      required: [ordemServicoId]
      properties:
        ordemServicoId:
          type: string
          format: uuid
        prioridade:
          type: integer
          minimum: 0
          default: 100
          description: Prioridade operacional da fila. Valores menores indicam maior urgencia.
    Execucao:
      allOf:
        - $ref: "#/components/schemas/ExecucaoCreateRequest"
        - type: object
          required: [execucaoId, status, prioridade, criadoEm, atualizadoEm, acoesPermitidas]
          properties:
            execucaoId:
              type: string
              format: uuid
            status:
              $ref: "#/components/schemas/StatusExecucao"
            diagnostico:
              type: string
            observacoesReparo:
              type: string
            criadoEm:
              type: string
              format: date-time
            atualizadoEm:
              type: string
              format: date-time
            acoesPermitidas:
              type: array
              items:
                $ref: "#/components/schemas/AcaoPermitidaExecucao"
    AcaoPermitidaExecucao:
      type: string
      enum: [INICIAR_DIAGNOSTICO, CONCLUIR_DIAGNOSTICO, INICIAR_REPARO, CONCLUIR_REPARO, CANCELAR]
    FilaExecucaoItem:
      allOf:
        - $ref: "#/components/schemas/Execucao"
        - type: object
          required: [posicao]
          properties:
            posicao:
              type: integer
              minimum: 1
              description: Posicao calculada na fila retornada, apos filtros e ordenacao por prioridade.
    StatusExecucao:
      type: string
      enum: [CRIADA, EM_DIAGNOSTICO, DIAGNOSTICO_CONCLUIDO, EM_REPARO, REPARO_CONCLUIDO, CANCELADA]
    ConclusaoDiagnosticoRequest:
      type: object
      properties:
        diagnostico:
          type: string
    ConclusaoReparoRequest:
      type: object
      properties:
        observacoes:
          type: string
    CancelamentoRequest:
      type: object
      properties:
        motivo:
          type: string
    ErrorResponse:
      type: object
      required: [timestamp, status, error, code, message, path, correlationId]
      properties:
        timestamp:
          type: string
          format: date-time
          example: "2026-06-23T15:30:00Z"
        status:
          type: integer
          example: 400
        error:
          type: string
          example: Bad Request
        code:
          type: string
          example: VALIDATION_ERROR
        message:
          type: string
          example: Campo obrigatorio ausente.
        path:
          type: string
          example: /api/v1/execucoes
        correlationId:
          type: string
          example: 7f4d0c4b6b8a4bb8a2f49bb8f06e1c3d
        requestId:
          type: string
          format: uuid
          example: d290f1ee-6c54-4b01-90e6-d701748f0851
        traceId:
          type: string
          example: 4bf92f3577b34da6a3ce929d0e0e4736
        spanId:
          type: string
          example: 00f067aa0ba902b7
        service:
          type: string
          example: oficina-execution-service
        logReference:
          type: string
          example: oficina-execution-service/2026-06-23/7f4d0c4b6b8a4bb8a2f49bb8f06e1c3d
        details:
          type: array
          items:
            $ref: "#/components/schemas/ErrorDetail"
    ErrorDetail:
      type: object
      required: [code, message]
      properties:
        field:
          type: string
          example: pecaId
        code:
          type: string
          example: REQUIRED
        message:
          type: string
          example: Campo obrigatorio.
